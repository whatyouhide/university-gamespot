Options +FollowSymLinks
RewriteEngine On

# Redirect every URL in the form of:
#   /a-controller/an_action.php
# to index.php, passing "a-controller" and "an_action".
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Regex:
# -- ^: beginning
# -- ([..]+): first capture group, which is the controller
# -- (?: ): a non capturing group containing an optional / or an optional
#    /some_action/, where the last / is optional itself.
RewriteRule ^([a-zA-Z0-9_\-]+)(?:/|/([a-zA-Z0-9_\-]+)/?)?$   index.php?controller=$1&action=$2 [QSA]
